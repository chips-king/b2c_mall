<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.b2cmall.employee.dao.mapper.LoginAuditMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_login_audit (shop_id, employee_id, username, remote_address, outcome, reason_code)
        VALUES (#{shopId}, #{employeeId}, #{username}, #{remoteAddress}, #{outcome}, #{reasonCode})
    </insert>
</mapper>
