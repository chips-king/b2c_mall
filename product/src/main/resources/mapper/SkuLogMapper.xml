<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.b2cmall.product.dao.mapper.SkuLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_sku_log (sku_id, shop_id, stock, price, status, created_user_id)
        VALUES (#{skuId}, #{shopId}, #{stock}, #{price}, #{status}, #{createdUserId})
    </insert>
</mapper>
