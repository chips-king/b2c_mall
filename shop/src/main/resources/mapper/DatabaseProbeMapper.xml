<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.b2cmall.shop.dao.mapper.DatabaseProbeMapper">
    <select id="probe" resultType="map">
        SELECT sqlite_version() AS sqliteVersion, datetime('now') AS databaseTime
    </select>
</mapper>
